{
  "name": "kairos-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "KÄIRŌS TASK MANAGER — multi-gerenciador de dispositivos tecnológicos",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "agent": "node agent/kairos-agent.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
